/// <reference types="react" />
import './styles.css';
import { TStyledTextProps } from './types';
declare const StyledText: ({ bold, center, children, className, color, fontFamily, fontSize, hasLeftSpace, hasRightSpace, highlighColor, indent, letterSpacing, lineSpacing, marginTop, numberOfLines, selectable, style, tabSize, tag, uppercase, ...others }: TStyledTextProps) => JSX.Element;
export default StyledText;
